# CLAUDE.md — 프로젝트 작업 지침

이 파일은 Claude Code가 이 저장소에서 작업할 때 자동으로 참조하는 지침입니다.
상세 명세는 `DEVELOPMENT.md`를 반드시 함께 읽으세요.

## 프로젝트

**액티브 인 서대문** — 서대문독립공원 야외 방탈출 모바일 웹앱. 8월 심훈 편.
참가자가 8개 거점을 돌며 미션을 풀고 "찢어진 시 조각"을 모아, 마지막에 사라진 독립운동가가 심훈임을 밝히는 게임.

## 기술 스택

- React 18 + Vite + TypeScript
- React Router v6 / Zustand (localStorage 연동)
- 모바일 세로 전용, PWA 지향
- 정적 호스팅 (Vercel/Netlify)

## 반드시 지킬 규칙

1. **정체 은폐**: "심훈", "그날이 오면"은 스팟 1~7의 어떤 화면·이미지 alt·소스 문자열에도 노출 금지. 최종 정체 공개 화면(`/reveal`)과 마지막 조각(스팟8)에서만 등장.
2. **조력자 난이도 동일**: 4인(유관순·안중근·윤봉길·김구)은 대사 문체만 다르고 정답·시간·힌트 정보량은 완전히 동일하게.
3. **데이터/로직 분리**: 게임 내용은 `src/data/`에만 둔다. 매달 인물 교체 시 데이터만 바꾸면 되도록. 컴포넌트·로직에 콘텐츠 하드코딩 금지.
4. **캐릭터·사진 원본 유지**: 조력자 4종, 8스팟 현장 사진은 변형·수정 없이 사용.
5. **초등·가족 대상**: 큰 버튼, 높은 대비, 짧은 문장. 오답 시 질책 없이 응원. 죽음·고문 직접 묘사 금지, 순사는 우스꽝스럽게.
6. **오프라인 대비**: 진행 상태는 localStorage 저장, 자산은 프리페치. 공원에서 통신 불안정 가정.

## 정답 값은 임시

`DEVELOPMENT.md` 4.5의 정답(116, 331919, 초록 등)은 **현장 답사 전 임시값**. `src/data/spots.ts` 한 곳에서만 수정 가능하게 유지하고, 코드 여기저기 흩뿌리지 말 것.

## 화면 흐름

표지 → 조력자 선택 → [장소안내 → 도착확인 → 쪽지 → 미션 → 조각획득] ×8 → 조각 배열 → 정체공개 → 보상

## 시각 레퍼런스

`active_in_seodaemun_app_full.html` (전체 목업)의 톤·레이아웃을 그대로 따를 것. 독립신문 지면 스타일. 색상/서체 토큰은 DEVELOPMENT.md 3장 참고.

## 작업 시

- 새 기능은 `DEVELOPMENT.md` 9장 구현 순서를 따를 것
- 커밋은 단계별로 작게
- 미션 컴포넌트는 `mission.type` 기준 분기 (discriminated union)
